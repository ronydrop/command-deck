<UserControl x:Class="CommandDeck.Controls.NotificationToast"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:CommandDeck.Converters"
             x:Name="ToastRoot"
             RenderTransformOrigin="1,0.5">

    <UserControl.Resources>
        <conv:NotificationTypeToBrushConverter x:Key="TypeToBrush" />
        <conv:NotificationSourceToIconConverter x:Key="SourceToIcon" />

        <Storyboard x:Key="SlideIn">
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                             From="380" To="0"
                             Duration="0:0:0.28">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase EasingMode="EaseOut" Exponent="4" />
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0" To="1"
                             Duration="0:0:0.2" />
        </Storyboard>
    </UserControl.Resources>

    <UserControl.RenderTransform>
        <TranslateTransform />
    </UserControl.RenderTransform>

    <!-- Toast card -->
    <Border x:Name="ToastBorder"
            Width="350"
            Background="{DynamicResource Surface0Brush}"
            CornerRadius="8"
            Margin="0,0,0,8"
            Cursor="Hand"
            SnapsToDevicePixels="True">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="2" BlurRadius="12" Opacity="0.35" Color="#000000" />
        </Border.Effect>

        <Grid>
            <!-- Left color stripe -->
            <Border CornerRadius="8,0,0,8"
                    Width="4"
                    HorizontalAlignment="Left"
                    Background="{Binding Type, Converter={StaticResource TypeToBrush}}" />

            <Grid Margin="12,10,8,10">
                <Grid.ColumnDefinitions>
                    <!-- Icon -->
                    <ColumnDefinition Width="Auto" />
                    <!-- Content -->
                    <ColumnDefinition Width="*" />
                    <!-- Dismiss -->
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Notification icon -->
                <TextBlock Grid.Column="0"
                           Text="{Binding Icon}"
                           FontSize="18"
                           VerticalAlignment="Top"
                           Margin="0,0,10,0" />

                <!-- Title + Message -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Title}"
                               Foreground="{DynamicResource TextBrush}"
                               FontSize="13"
                               FontWeight="SemiBold"
                               TextTrimming="CharacterEllipsis"
                               MaxWidth="250" />
                    <TextBlock Text="{Binding Message}"
                               Foreground="{DynamicResource SubtextBrush}"
                               FontSize="11"
                               TextWrapping="Wrap"
                               MaxHeight="40"
                               Margin="0,2,0,0"
                               Visibility="{Binding Message, Converter={StaticResource NullToVis}}" />
                </StackPanel>

                <!-- Dismiss button -->
                <Button Grid.Column="2"
                        Style="{StaticResource IconButton}"
                        Width="24" Height="24"
                        VerticalAlignment="Top"
                        Command="{Binding DataContext.DismissNotificationCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                        CommandParameter="{Binding Id}"
                        ToolTip="Fechar">
                    <Path Data="{StaticResource CloseIcon}"
                          Stroke="{DynamicResource Overlay0Brush}"
                          StrokeThickness="1.5"
                          StrokeLineJoin="Round" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                          Stretch="Uniform"
                          Width="10" Height="10" />
                </Button>
            </Grid>
        </Grid>
    </Border>
</UserControl>
