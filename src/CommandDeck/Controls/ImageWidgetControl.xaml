<UserControl x:Class="CommandDeck.Controls.ImageWidgetControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="400" d:DesignHeight="300"
             AllowDrop="True"
             Drop="OnDrop"
             DragOver="OnDragOver">

    <Grid>
        <!-- Image display -->
        <Image x:Name="DisplayImage"
               Source="{Binding ImageSource}"
               Stretch="Uniform"
               Opacity="{Binding ImageOpacity}"
               RenderOptions.BitmapScalingMode="HighQuality"
               Margin="2" />

        <!-- Empty state: drop/paste hint -->
        <StackPanel VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    IsHitTestVisible="False">
            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ImageSource}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>
            <TextBlock Text="🖼"
                       FontSize="32"
                       HorizontalAlignment="Center"
                       Opacity="0.4"
                       Margin="0,0,0,8" />
            <TextBlock Text="Cole (Ctrl+V) ou arraste"
                       FontSize="12"
                       Foreground="{DynamicResource SubtextBrush}"
                       HorizontalAlignment="Center" />
            <TextBlock Text="uma imagem aqui"
                       FontSize="12"
                       Foreground="{DynamicResource SubtextBrush}"
                       HorizontalAlignment="Center"
                       Margin="0,2,0,0" />
        </StackPanel>

        <!-- Opacity slider (bottom, small) -->
        <StackPanel VerticalAlignment="Bottom"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal"
                    Margin="6">
            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Setter Property="Opacity" Value="0" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=IsMouseOver}"
                                     Value="True">
                            <Setter Property="Opacity" Value="1" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>
            <TextBlock Text="Opacidade"
                       FontSize="10"
                       Foreground="{DynamicResource SubtextBrush}"
                       VerticalAlignment="Center"
                       Margin="0,0,4,0" />
            <Slider Width="80"
                    Minimum="0.1" Maximum="1.0"
                    Value="{Binding ImageOpacity}"
                    VerticalAlignment="Center"
                    SmallChange="0.05"
                    LargeChange="0.1"
                    TickFrequency="0.1"
                    IsSnapToTickEnabled="False" />
        </StackPanel>
    </Grid>
</UserControl>
